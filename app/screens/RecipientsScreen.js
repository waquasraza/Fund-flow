import { StyleSheet, View, Text } from 'react-native';

const RecipientsScreen = () => {
	return (
		<View style={styles.container}>
			<Text>RecipientsScreen</Text>
		</View>
	);
};

export default RecipientsScreen;

const styles = StyleSheet.create({
	container: {},
});
